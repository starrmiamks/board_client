import React, { FC } from 'react';

type HomeProps = {};

const Home: FC<HomeProps> = (props) => {
    return (
        <div>
            <h5>Welcome to Board</h5>
        </div>
    );
};

export default Home; 